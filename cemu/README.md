# CEMU

Status: initial capsule materialization

CEMU is the operational capsule layer currently materialized in this repository.

## Present in tree
- `core/`
- `builders/`
- `outputs/`
- `templates/`

## Current interpretation
- `core/` contains initial operational capsule modules
- `builders/` contains initial bridge builders
- `outputs/` and `templates/` are present as structure and still require fuller population

This layer should be read as an initial real capsule materialization, not yet as a final complete capsule.
