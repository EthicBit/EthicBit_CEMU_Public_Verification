name: slsa-hybrid-attest

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    tags:
      - "v*"
      - "audit-freeze-*"

permissions:
  contents: read
  id-token: write
  attestations: write

env:
  PYTHON_VERSION: "3.11"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  ETHICBIT_ME_GATE_REQUIRED_SECTORS: CORE,JUSTICIA,FINANZAS,SECURITY,TECHNICAL,LEGAL,REGULATORY
  ETHICBIT_HERMETIC_BASE_IMAGE_REF: ${{ vars.ETHICBIT_HERMETIC_BASE_IMAGE_REF }}
  ETHICBIT_SIGNING_BACKEND: ${{ vars.ETHICBIT_SIGNING_BACKEND }}
  ETHICBIT_SIGNING_ENDPOINT: ${{ vars.ETHICBIT_SIGNING_ENDPOINT }}
  ETHICBIT_SIGNING_DISABLE_PROXY: ${{ vars.ETHICBIT_SIGNING_DISABLE_PROXY }}
  ETHICBIT_REMOTE_ED25519_KEY_ID: ${{ vars.ETHICBIT_ED25519_KEY_ID }}
  ETHICBIT_REMOTE_MLDSA_KEY_ID: ${{ vars.ETHICBIT_MLDSA_KEY_ID }}
  ETHICBIT_REMOTE_PROVIDER_SUPPORTS_MLDSA: ${{ vars.ETHICBIT_REMOTE_PROVIDER_SUPPORTS_MLDSA }}
  ETHICBIT_REMOTE_MLDSA_EFFECTIVE_MODE: ${{ vars.ETHICBIT_REMOTE_MLDSA_EFFECTIVE_MODE }}

jobs:
  build-sign-attest:
    runs-on: ${{ ((github.ref_type == 'branch' && github.ref_name == 'main') || (github.ref_type == 'tag' && (startsWith(github.ref_name, 'v') || startsWith(github.ref_name, 'audit-freeze-')))) && fromJSON('["self-hosted","linux","x64","mldsa-native"]') || fromJSON('["ubuntu-latest"]') }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Reclaim Docker space (self-hosted)
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${RUNNER_ENVIRONMENT:-}" == "self-hosted" ]]; then
            echo "Self-hosted runner detected: pruning Docker caches before hermetic build."
            docker system df || true
            docker system prune -af --volumes || true
            docker builder prune -af || true
            docker system df || true
          else
            echo "GitHub-hosted runner: skipping Docker prune."
          fi

      - name: Materialize hermetic build posture report
        shell: bash
        run: |
          set -euo pipefail
          export PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin:${PATH:-}"
          mkdir -p results

          strict_required="1"
          mode="strict_hermetic"
          network_mode="none"
          build_system="docker"
          build_target="hermetic-check"
          declared_gap=""
          base_image_ref="${ETHICBIT_HERMETIC_BASE_IMAGE_REF:-}"
          docker_bin="$(command -v docker || true)"
          if [[ -z "${docker_bin}" && -x /usr/bin/docker ]]; then
            docker_bin="/usr/bin/docker"
          fi
          if [[ -z "${docker_bin}" && -x /snap/bin/docker ]]; then
            docker_bin="/snap/bin/docker"
          fi

          [[ "$base_image_ref" =~ @sha256:[a-f0-9]{64}$ ]] || {
            echo "ETHICBIT_HERMETIC_BASE_IMAGE_REF must be pinned with @sha256 for strict hermetic mode." >&2
            exit 1
          }
          if [[ -z "${docker_bin}" ]]; then
            echo "Docker binary not found for strict hermetic mode." >&2
            echo "PATH=$PATH" >&2
            ls -l /usr/bin/docker /snap/bin/docker >&2 2>/dev/null || true
            exit 127
          fi

          "${docker_bin}" build \
            --file Dockerfile.build \
            --target hermetic-check \
            --build-arg ETHICBIT_BASE_IMAGE_REF="$base_image_ref" \
            --network=none \
            --tag "ethicbit-hermetic-probe:${GITHUB_RUN_ID}" \
            .

          extra_args=(--strict-required)

          python3 scripts/assurance/write_hermetic_build_report.py \
            --output results/hermetic_build_report.json \
            --mode "$mode" \
            --base-image-ref "$base_image_ref" \
            --network-mode "$network_mode" \
            --claim-level "ci_grade" \
            --build-system "$build_system" \
            --build-target "$build_target" \
            --lockfile package-lock.json \
            --lockfile requirements.txt \
            "${extra_args[@]}"

          if [[ "${RUNNER_ENVIRONMENT:-}" == "self-hosted" ]]; then
            echo "Reclaiming Docker space after hermetic build."
            "${docker_bin}" image rm -f "ethicbit-hermetic-probe:${GITHUB_RUN_ID}" || true
            "${docker_bin}" system prune -af --volumes || true
            "${docker_bin}" builder prune -af || true
            "${docker_bin}" system df || true
          fi

          {
            echo "ETHICBIT_HERMETIC_REQUIRE_STRICT=${strict_required}"
            echo "ETHICBIT_HERMETIC_REPORT_PATH=results/hermetic_build_report.json"
          } >> "$GITHUB_ENV"

      - name: Show tool versions
        run: |
          python3 --version
          openssl version
          node --version || true
          npm --version || true

      - name: Prepare directories
        run: |
          mkdir -p provenance
          mkdir -p artifacts/ci
          mkdir -p attestation
          mkdir -p assurance/keys
          chmod +x assurance/signers/sign_dispatch.sh scripts/security/verify_key_posture.sh scripts/security/rotate_keys_transitional.sh || true

      - name: Prepare hybrid signing keys (secrets + compatibility fallback)
        shell: bash
        run: |
          set -euo pipefail
          signing_backend="${ETHICBIT_SIGNING_BACKEND:-github_secrets_pem}"
          if [[ -z "${signing_backend}" ]]; then
            signing_backend="github_secrets_pem"
          fi
          if [[ "${signing_backend}" != "github_secrets_pem" && "${signing_backend}" != "remote_signing_provider" ]]; then
            echo "Unsupported ETHICBIT_SIGNING_BACKEND=${signing_backend}" >&2
            exit 1
          fi

          is_sovereign_ref="0"
          if [[ "${GITHUB_REF_TYPE}" == "branch" && "${GITHUB_REF_NAME}" == "main" ]]; then
            is_sovereign_ref="1"
          fi
          if [[ "${GITHUB_REF_TYPE}" == "tag" && ( "${GITHUB_REF_NAME}" == v* || "${GITHUB_REF_NAME}" == audit-freeze-* ) ]]; then
            is_sovereign_ref="1"
          fi

          ed25519_key_source="unknown"
          mldsa_key_source="unknown"
          mldsa_effective_mode="native_mldsa"
          runner_supports_mldsa="true"
          ed25519_key_id=""
          mldsa_key_id=""

          if [[ "${signing_backend}" == "remote_signing_provider" ]]; then
            ed25519_key_source="trusted_remote_provider"
            mldsa_key_source="trusted_remote_provider"
            mldsa_effective_mode="${ETHICBIT_REMOTE_MLDSA_EFFECTIVE_MODE:-native_mldsa}"
            if [[ -z "${mldsa_effective_mode}" ]]; then
              mldsa_effective_mode="native_mldsa"
            fi
            runner_supports_mldsa="${ETHICBIT_REMOTE_PROVIDER_SUPPORTS_MLDSA:-true}"
            if [[ -z "${runner_supports_mldsa}" ]]; then
              runner_supports_mldsa="true"
            fi

            ed25519_key_id="${ETHICBIT_REMOTE_ED25519_KEY_ID:-}"
            mldsa_key_id="${ETHICBIT_REMOTE_MLDSA_KEY_ID:-}"

            if [[ -z "${ed25519_key_id}" || -z "${mldsa_key_id}" ]]; then
              echo "Policy violation: remote_signing_provider requires remote key ids for both channels." >&2
              exit 1
            fi
            if [[ -z "${ETHICBIT_SIGNING_ENDPOINT:-}" ]]; then
              echo "Policy violation: remote_signing_provider requires ETHICBIT_SIGNING_ENDPOINT." >&2
              exit 1
            fi
            if [[ -z "${{ secrets.ETHICBIT_SIGNING_TOKEN }}" ]]; then
              echo "Policy violation: remote_signing_provider requires ETHICBIT_SIGNING_TOKEN secret." >&2
              exit 1
            fi

            # Remote signing keeps private keys non-exportable; verification still needs public keys.
            [[ -n "${{ secrets.ETHICBIT_ED25519_PUBLIC_KEY_PEM }}" ]] || { echo "Missing ETHICBIT_ED25519_PUBLIC_KEY_PEM for remote verification." >&2; exit 1; }
            [[ -n "${{ secrets.ETHICBIT_MLDSA_PUBLIC_KEY_PEM }}" ]] || { echo "Missing ETHICBIT_MLDSA_PUBLIC_KEY_PEM for remote verification." >&2; exit 1; }
            printf '%s' "${{ secrets.ETHICBIT_ED25519_PUBLIC_KEY_PEM }}" > assurance/keys/ed25519_public.pem
            printf '%s' "${{ secrets.ETHICBIT_MLDSA_PUBLIC_KEY_PEM }}" > assurance/keys/mldsa_public.pem
            rm -f assurance/keys/ed25519_private.pem assurance/keys/mldsa_private.pem
          else
            ed25519_key_source="trusted_secrets"
            mldsa_key_source="trusted_secrets"

            if [[ -n "${{ secrets.ETHICBIT_ED25519_PRIVATE_KEY_PEM }}" && -n "${{ secrets.ETHICBIT_ED25519_PUBLIC_KEY_PEM }}" ]]; then
              printf '%s' "${{ secrets.ETHICBIT_ED25519_PRIVATE_KEY_PEM }}" > assurance/keys/ed25519_private.pem
              printf '%s' "${{ secrets.ETHICBIT_ED25519_PUBLIC_KEY_PEM }}" > assurance/keys/ed25519_public.pem
            else
              if [[ "${is_sovereign_ref}" == "1" ]]; then
                echo "Policy violation: sovereign ref requires trusted ED25519 secrets." >&2
                exit 1
              fi
              ed25519_key_source="ephemeral_runner"
              openssl genpkey -algorithm Ed25519 -out assurance/keys/ed25519_private.pem
              openssl pkey -in assurance/keys/ed25519_private.pem -pubout -out assurance/keys/ed25519_public.pem
            fi

            if [[ -n "${{ secrets.ETHICBIT_MLDSA_PRIVATE_KEY_PEM }}" && -n "${{ secrets.ETHICBIT_MLDSA_PUBLIC_KEY_PEM }}" ]]; then
              printf '%s' "${{ secrets.ETHICBIT_MLDSA_PRIVATE_KEY_PEM }}" > assurance/keys/mldsa_private.pem
              printf '%s' "${{ secrets.ETHICBIT_MLDSA_PUBLIC_KEY_PEM }}" > assurance/keys/mldsa_public.pem
            else
              if [[ "${is_sovereign_ref}" == "1" ]]; then
                echo "Policy violation: sovereign ref requires trusted ML-DSA secrets." >&2
                exit 1
              fi
              mldsa_key_source="ephemeral_runner"
              mldsa_effective_mode="compatibility_fallback"
              runner_supports_mldsa="false"
              openssl genpkey -algorithm Ed25519 -out assurance/keys/mldsa_private.pem
              openssl pkey -in assurance/keys/mldsa_private.pem -pubout -out assurance/keys/mldsa_public.pem
            fi

            for key_file in assurance/keys/ed25519_private.pem assurance/keys/ed25519_public.pem assurance/keys/mldsa_private.pem assurance/keys/mldsa_public.pem; do
              if [[ ! -s "${key_file}" ]]; then
                echo "Missing signing key material: ${key_file}" >&2
                exit 1
              fi
            done

            printf '{"artifactType":"ci_key_probe"}' >/tmp/ci_key_probe.json
            openssl pkeyutl -sign -inkey assurance/keys/ed25519_private.pem -rawin -in /tmp/ci_key_probe.json -out /tmp/ci_key_probe.ed.sig

            # Some GitHub runners do not support ML-DSA keys in OpenSSL.
            # Fallback to a CI-compatible keypair so fail-closed controls remain executable.
            if ! openssl pkeyutl -sign -inkey assurance/keys/mldsa_private.pem -rawin -in /tmp/ci_key_probe.json -out /tmp/ci_key_probe.mldsa.sig >/tmp/ci_key_probe.mldsa.err 2>&1; then
              if [[ "${is_sovereign_ref}" == "1" ]]; then
                echo "Policy violation: sovereign ref requires native ML-DSA support on runner (compatibility fallback disabled)." >&2
                cat /tmp/ci_key_probe.mldsa.err >&2 || true
                exit 1
              fi
              echo "ML-DSA key unsupported on runner; enabling CI compatibility fallback."
              mldsa_effective_mode="compatibility_fallback"
              mldsa_key_source="ephemeral_runner"
              runner_supports_mldsa="false"
              openssl genpkey -algorithm Ed25519 -out assurance/keys/mldsa_private.pem
              openssl pkey -in assurance/keys/mldsa_private.pem -pubout -out assurance/keys/mldsa_public.pem
              openssl pkeyutl -sign -inkey assurance/keys/mldsa_private.pem -rawin -in /tmp/ci_key_probe.json -out /tmp/ci_key_probe.mldsa.sig
            fi
          fi

          if [[ ! -s assurance/keys/ed25519_public.pem || ! -s assurance/keys/mldsa_public.pem ]]; then
            echo "Missing signing public key material for verification." >&2
            exit 1
          fi

          if [[ "${mldsa_effective_mode}" == "native_mldsa" ]]; then
            crypto_mode_effective="native_hybrid"
          else
            crypto_mode_effective="compatibility_fallback"
          fi

          if [[ "${mldsa_effective_mode}" == "native_mldsa" && "${ed25519_key_source}" != "ephemeral_runner" && "${mldsa_key_source}" != "ephemeral_runner" ]]; then
            hybrid_claim_mode="native_hybrid"
            crypto_claim_tier="strict_trusted_hybrid"
            if [[ "${ed25519_key_source}" == "trusted_remote_provider" && "${mldsa_key_source}" == "trusted_remote_provider" ]]; then
              key_provenance_mode="fully_trusted_remote_non_exportable"
            else
              key_provenance_mode="fully_trusted_native"
            fi
          else
            hybrid_claim_mode="compatibility_hybrid"
            if [[ "${ed25519_key_source}" == "trusted_secrets" && "${mldsa_key_source}" == "ephemeral_runner" && "${mldsa_effective_mode}" == "compatibility_fallback" ]]; then
              key_provenance_mode="trusted_plus_compatibility_fallback"
              crypto_claim_tier="compatibility_hybrid_trusted_anchor"
            elif [[ "${ed25519_key_source}" == "trusted_secrets" && "${mldsa_key_source}" == "ephemeral_runner" ]]; then
              key_provenance_mode="trusted_plus_ephemeral_mldsa"
              crypto_claim_tier="compatibility_hybrid_mixed"
            elif [[ "${ed25519_key_source}" == "ephemeral_runner" && "${mldsa_key_source}" == "trusted_secrets" ]]; then
              key_provenance_mode="ephemeral_ed25519_plus_trusted_mldsa"
              crypto_claim_tier="compatibility_hybrid_mixed"
            elif [[ "${ed25519_key_source}" == "ephemeral_runner" && "${mldsa_key_source}" == "ephemeral_runner" ]]; then
              key_provenance_mode="fully_ephemeral"
              crypto_claim_tier="compatibility_hybrid_ephemeral"
            elif [[ "${ed25519_key_source}" == "trusted_remote_provider" && "${mldsa_key_source}" == "trusted_remote_provider" ]]; then
              key_provenance_mode="trusted_remote_non_exportable_compatibility"
              crypto_claim_tier="compatibility_hybrid_remote"
            else
              key_provenance_mode="mixed_provenance"
              crypto_claim_tier="compatibility_hybrid_mixed"
            fi
          fi

          if [[ -z "${ed25519_key_id}" ]]; then
            if [[ "${ed25519_key_source}" == "trusted_secrets" ]]; then
              ed25519_key_id="ETHICBIT_ED25519_CI_SECRET_V1"
            else
              ed25519_key_id="ETHICBIT_ED25519_CI_EPHEMERAL_V1"
            fi
          fi

          if [[ -z "${mldsa_key_id}" ]]; then
            if [[ "${mldsa_effective_mode}" == "native_mldsa" ]]; then
              if [[ "${mldsa_key_source}" == "trusted_secrets" ]]; then
                mldsa_key_id="ETHICBIT_MLDSA65_CI_SECRET_V1"
              else
                mldsa_key_id="ETHICBIT_MLDSA65_CI_EPHEMERAL_V1"
              fi
            else
              mldsa_key_id="ETHICBIT_MLDSA_COMPAT_ED25519_CI_V1"
            fi
          fi

          if [[ "${ed25519_key_source}" == "ephemeral_runner" || "${mldsa_key_source}" == "ephemeral_runner" ]]; then
            ephemeral_keys_used="true"
          else
            ephemeral_keys_used="false"
          fi

          if [[ "${hybrid_claim_mode}" == "native_hybrid" && "${ephemeral_keys_used}" == "false" && "${runner_supports_mldsa}" == "true" ]]; then
            eligible_for_sovereign_release="true"
          else
            eligible_for_sovereign_release="false"
          fi

          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            audit_require_hybrid="1"
          elif [[ "${GITHUB_REF_TYPE}" == "branch" && "${GITHUB_REF_NAME}" == "main" ]]; then
            audit_require_hybrid="1"
          else
            audit_require_hybrid="0"
          fi

          release_class="branch_ci"
          if [[ "${GITHUB_REF_TYPE}" == "tag" && "${GITHUB_REF_NAME}" == audit-freeze-* ]]; then
            release_class="audit_freeze_tag"
          elif [[ "${GITHUB_REF_TYPE}" == "tag" && "${GITHUB_REF_NAME}" == v* ]]; then
            release_class="release_tag"
          fi

          claim_level="ci_grade"
          require_native_hybrid="0"
          if [[ "${release_class}" == "audit_freeze_tag" ]]; then
            claim_level="freeze_grade"
            require_native_hybrid="1"
          elif [[ "${release_class}" == "release_tag" ]]; then
            claim_level="sovereign_release"
            require_native_hybrid="1"
          fi

          require_trusted_keys="0"
          if [[ "${GITHUB_REF_TYPE}" == "branch" && "${GITHUB_REF_NAME}" == "main" ]]; then
            require_trusted_keys="1"
            require_native_hybrid="1"
          fi
          if [[ "${claim_level}" == "freeze_grade" || "${claim_level}" == "sovereign_release" ]]; then
            require_trusted_keys="1"
          fi

          if [[ "${signing_backend}" == "remote_signing_provider" ]]; then
            require_trusted_keys="1"
          fi

          require_go_gate="0"
          if [[ "${GITHUB_REF_TYPE}" == "branch" && "${GITHUB_REF_NAME}" == "main" ]]; then
            require_go_gate="1"
          fi
          if [[ "${claim_level}" == "freeze_grade" || "${claim_level}" == "sovereign_release" ]]; then
            require_go_gate="1"
          fi

          {
            echo "ETHICBIT_KEY_PROVENANCE_MODE=${key_provenance_mode}"
            echo "ETHICBIT_ED25519_KEY_SOURCE=${ed25519_key_source}"
            echo "ETHICBIT_MLDSA_KEY_SOURCE=${mldsa_key_source}"
            echo "ETHICBIT_MLDSA_MODE=${mldsa_effective_mode}"
            echo "ETHICBIT_MLDSA_EFFECTIVE_MODE=${mldsa_effective_mode}"
            echo "ETHICBIT_CRYPTO_MODE_EFFECTIVE=${crypto_mode_effective}"
            echo "ETHICBIT_HYBRID_CLAIM_MODE=${hybrid_claim_mode}"
            echo "ETHICBIT_CRYPTO_CLAIM_TIER=${crypto_claim_tier}"
            echo "ETHICBIT_ED25519_KEY_ID=${ed25519_key_id}"
            echo "ETHICBIT_MLDSA_KEY_ID=${mldsa_key_id}"
            echo "ETHICBIT_RUNNER_SUPPORTS_MLDSA=${runner_supports_mldsa}"
            echo "ETHICBIT_EPHEMERAL_KEYS_USED=${ephemeral_keys_used}"
            echo "ETHICBIT_ELIGIBLE_FOR_SOVEREIGN_RELEASE=${eligible_for_sovereign_release}"
            echo "ETHICBIT_AUDIT_REQUIRE_HYBRID=${audit_require_hybrid}"
            echo "ETHICBIT_CLAIM_LEVEL=${claim_level}"
            echo "ETHICBIT_REQUIRE_NATIVE_HYBRID=${require_native_hybrid}"
            echo "ETHICBIT_REQUIRE_TRUSTED_KEYS=${require_trusted_keys}"
            echo "ETHICBIT_REQUIRE_GO_GATE=${require_go_gate}"
            echo "ETHICBIT_RELEASE_CLASS=${release_class}"
            echo "ETHICBIT_SIGNING_BACKEND=${signing_backend}"
          } >> "$GITHUB_ENV"

          chmod 644 assurance/keys/*_public.pem || true
          if [[ "${signing_backend}" == "github_secrets_pem" ]]; then
            chmod 600 assurance/keys/*_private.pem || true
          fi

      - name: Export signing environment
        shell: bash
        run: |
          {
            echo "ETHICBIT_ED25519_SIGN_CMD=${{ github.workspace }}/assurance/signers/sign_dispatch.sh ED25519 {payload}"
            echo "ETHICBIT_MLDSA_SIGN_CMD=${{ github.workspace }}/assurance/signers/sign_dispatch.sh ML-DSA {payload}"
            echo "ETHICBIT_ED25519_VERIFY_CMD=${{ github.workspace }}/assurance/signers/ed25519_verify.sh {payload} {signature}"
            echo "ETHICBIT_MLDSA_VERIFY_CMD=${{ github.workspace }}/assurance/signers/mldsa_verify.sh {payload} {signature}"
            echo "ETHICBIT_ED25519_PRIVATE_KEY=${{ github.workspace }}/assurance/keys/ed25519_private.pem"
            echo "ETHICBIT_ED25519_PUBLIC_KEY=${{ github.workspace }}/assurance/keys/ed25519_public.pem"
            echo "ETHICBIT_MLDSA_PRIVATE_KEY=${{ github.workspace }}/assurance/keys/mldsa_private.pem"
            echo "ETHICBIT_MLDSA_PUBLIC_KEY=${{ github.workspace }}/assurance/keys/mldsa_public.pem"
            echo "ETHICBIT_SIGNING_BACKEND=${ETHICBIT_SIGNING_BACKEND:-github_secrets_pem}"
            echo "ETHICBIT_SIGNING_ENDPOINT=${ETHICBIT_SIGNING_ENDPOINT:-}"
            echo "ETHICBIT_SIGNING_DISABLE_PROXY=${ETHICBIT_SIGNING_DISABLE_PROXY:-1}"
            echo "ETHICBIT_SIGNING_INCLUDE_PAYLOAD_PATH=${ETHICBIT_SIGNING_INCLUDE_PAYLOAD_PATH:-0}"
          } >> "$GITHUB_ENV"

          if [[ -n "${{ secrets.ETHICBIT_SIGNING_TOKEN }}" ]]; then
            echo "ETHICBIT_SIGNING_TOKEN=${{ secrets.ETHICBIT_SIGNING_TOKEN }}" >> "$GITHUB_ENV"
          fi

      - name: Write signing mode artifacts
        shell: bash
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          report = {
            "artifactType": "signing_environment_report",
            "repository": os.environ.get("GITHUB_REPOSITORY", ""),
            "ref": os.environ.get("GITHUB_REF", ""),
            "sha": os.environ.get("GITHUB_SHA", ""),
            "run_id": os.environ.get("GITHUB_RUN_ID", ""),
            "signing_backend": os.environ.get("ETHICBIT_SIGNING_BACKEND", "github_secrets_pem"),
            "signing_endpoint_configured": bool(os.environ.get("ETHICBIT_SIGNING_ENDPOINT", "").strip()),
            "ed25519_key_source": os.environ.get("ETHICBIT_ED25519_KEY_SOURCE", "unknown"),
            "mldsa_key_source": os.environ.get("ETHICBIT_MLDSA_KEY_SOURCE", "unknown"),
            "mldsa_effective_mode": os.environ.get("ETHICBIT_MLDSA_EFFECTIVE_MODE", "unknown"),
            "hybrid_claim_mode": os.environ.get("ETHICBIT_HYBRID_CLAIM_MODE", "unknown"),
            "ed25519_key_id": os.environ.get("ETHICBIT_ED25519_KEY_ID", "unknown"),
            "mldsa_key_id": os.environ.get("ETHICBIT_MLDSA_KEY_ID", "unknown"),
            "audit_require_hybrid": os.environ.get("ETHICBIT_AUDIT_REQUIRE_HYBRID", "0"),
            "claim_level": os.environ.get("ETHICBIT_CLAIM_LEVEL", "ci_grade"),
            "require_native_hybrid": os.environ.get("ETHICBIT_REQUIRE_NATIVE_HYBRID", "0"),
            "require_trusted_keys": os.environ.get("ETHICBIT_REQUIRE_TRUSTED_KEYS", "0"),
            "release_class": os.environ.get("ETHICBIT_RELEASE_CLASS", "branch_ci"),
          }

          effective = {
            "artifactType": "hybrid_mode_effective",
            "crypto_mode_effective": os.environ.get("ETHICBIT_CRYPTO_MODE_EFFECTIVE", "unknown"),
            "ed25519_key_source": os.environ.get("ETHICBIT_ED25519_KEY_SOURCE", "unknown"),
            "mldsa_key_source": os.environ.get("ETHICBIT_MLDSA_KEY_SOURCE", "unknown"),
            "mldsa_effective_mode": os.environ.get("ETHICBIT_MLDSA_EFFECTIVE_MODE", "unknown"),
            "hybrid_claim_mode": os.environ.get("ETHICBIT_HYBRID_CLAIM_MODE", "unknown"),
            "key_provenance_mode": os.environ.get("ETHICBIT_KEY_PROVENANCE_MODE", "unknown"),
            "claim_tier": os.environ.get("ETHICBIT_CRYPTO_CLAIM_TIER", "unspecified"),
          }

          truth = {
            "artifactType": "hybrid_crypto_truth",
            "signing_backend": os.environ.get("ETHICBIT_SIGNING_BACKEND", "github_secrets_pem"),
            "ed25519_key_source": os.environ.get("ETHICBIT_ED25519_KEY_SOURCE", "unknown"),
            "mldsa_key_source": os.environ.get("ETHICBIT_MLDSA_KEY_SOURCE", "unknown"),
            "mldsa_effective_mode": os.environ.get("ETHICBIT_MLDSA_EFFECTIVE_MODE", "unknown"),
            "hybrid_claim_mode": os.environ.get("ETHICBIT_HYBRID_CLAIM_MODE", "unknown"),
            "key_provenance_mode": os.environ.get("ETHICBIT_KEY_PROVENANCE_MODE", "unknown"),
            "crypto_claim_tier": os.environ.get("ETHICBIT_CRYPTO_CLAIM_TIER", "unspecified"),
            "release_class": os.environ.get("ETHICBIT_RELEASE_CLASS", "branch_ci"),
            "ephemeral_keys_used": os.environ.get("ETHICBIT_EPHEMERAL_KEYS_USED", "true").lower() == "true",
            "runner_supports_mldsa": os.environ.get("ETHICBIT_RUNNER_SUPPORTS_MLDSA", "false").lower() == "true",
            "claim_level": os.environ.get("ETHICBIT_CLAIM_LEVEL", "ci_grade"),
            "require_native_hybrid": os.environ.get("ETHICBIT_REQUIRE_NATIVE_HYBRID", "0").lower() in {"1", "true", "yes"},
            "require_trusted_keys": os.environ.get("ETHICBIT_REQUIRE_TRUSTED_KEYS", "0").lower() in {"1", "true", "yes"},
            "eligible_for_sovereign_release": os.environ.get("ETHICBIT_ELIGIBLE_FOR_SOVEREIGN_RELEASE", "false").lower() == "true",
          }

          Path("artifacts/ci/signing_environment_report.json").write_text(
              json.dumps(report, indent=2, ensure_ascii=False) + "\n",
              encoding="utf-8",
          )
          Path("artifacts/ci/hybrid_mode_effective.json").write_text(
              json.dumps(effective, indent=2, ensure_ascii=False) + "\n",
              encoding="utf-8",
          )
          Path("artifacts/ci/hybrid_crypto_truth.json").write_text(
              json.dumps(truth, indent=2, ensure_ascii=False) + "\n",
              encoding="utf-8",
          )
          PY

      - name: Verify key posture
        shell: bash
        run: |
          set -euo pipefail
          scripts/security/verify_key_posture.sh \
            --output results/key_posture_report.json \
            --repo "${{ github.repository }}" \
            --probe-signing
          key_posture_status="$(python3 - <<'PY'
          import json
          from pathlib import Path
          path = Path("results/key_posture_report.json")
          if not path.exists():
              raise SystemExit("Missing results/key_posture_report.json")
          payload = json.loads(path.read_text(encoding="utf-8"))
          status = str(payload.get("status") or "").strip()
          if not status:
              raise SystemExit("Missing key posture status")
          print(status)
          PY
          )"
          {
            echo "ETHICBIT_KEY_POSTURE_STATUS=${key_posture_status}"
            echo "ETHICBIT_KEY_POSTURE_REPORT_PATH=${{ github.workspace }}/results/key_posture_report.json"
          } >> "$GITHUB_ENV"

      - name: Diagnose signing environment
        shell: bash
        run: |
          set -e
          echo "===== OPENSSL ====="
          which openssl
          openssl version
          openssl list -public-key-algorithms | grep -i 'ML-DSA\|MLDSA' || true

          echo
          echo "===== KEY FILES ====="
          ls -l assurance/keys || true
          wc -c assurance/keys/ed25519_private.pem assurance/keys/ed25519_public.pem assurance/keys/mldsa_private.pem assurance/keys/mldsa_public.pem || true

          echo
          echo "===== TEST PAYLOAD ====="
          python3 - <<'PY'
          import json
          from pathlib import Path
          from scripts.crypto.jcs_rfc8785 import canonicalize_bytes

          payload = {
            "artifactType": "ci_sign_test",
            "buildId": "github-actions",
            "claim": "ETHICBIT_CI_SIGN_TEST"
          }
          Path("/tmp/ci_test_payload.jcs.json").write_bytes(canonicalize_bytes(payload))
          print("wrote test payload")
          PY

          echo
          echo "===== SIGNER DISPATCH PROBE ====="
          if [[ "${ETHICBIT_SIGNING_BACKEND:-github_secrets_pem}" == "remote_signing_provider" ]]; then
            echo "Backend=remote_signing_provider"
          else
            echo "Backend=github_secrets_pem"
          fi
          bash assurance/signers/sign_dispatch.sh ED25519 /tmp/ci_test_payload.jcs.json | head -c 120 ; echo
          if bash assurance/signers/sign_dispatch.sh ML-DSA /tmp/ci_test_payload.jcs.json > /tmp/mldsa_ci_test.sig 2>/tmp/mldsa_ci_test.err; then
            head -c 120 /tmp/mldsa_ci_test.sig ; echo
            echo "MLDSA_CI=PASS"
          else
            cat /tmp/mldsa_ci_test.err || true
            echo "MLDSA_CI=FAIL"
            exit 1
          fi

      - name: Setup Go (GitHub-hosted)
        if: ${{ runner.environment != 'self-hosted' }}
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"
          cache: false

      - name: Bootstrap Go toolchain (self-hosted if missing)
        if: ${{ runner.environment == 'self-hosted' }}
        shell: bash
        run: |
          set -euo pipefail
          if command -v go >/dev/null 2>&1; then
            echo "Using preinstalled Go toolchain on self-hosted runner."
            go version
            exit 0
          fi

          go_version="1.25.1"
          arch="$(uname -m)"
          case "${arch}" in
            x86_64|amd64) go_arch="amd64" ;;
            aarch64|arm64) go_arch="arm64" ;;
            *)
              echo "Unsupported self-hosted architecture for Go bootstrap: ${arch}" >&2
              exit 1
              ;;
          esac

          go_tgz="go${go_version}.linux-${go_arch}.tar.gz"
          go_url="https://go.dev/dl/${go_tgz}"
          go_root="${RUNNER_TEMP:-/tmp}/ethicbit-go-toolchain"
          rm -rf "${go_root}"
          mkdir -p "${go_root}"

          curl --fail --location --retry 3 --connect-timeout 20 --max-time 300 "${go_url}" -o "${go_root}/${go_tgz}"
          tar -C "${go_root}" -xzf "${go_root}/${go_tgz}"
          echo "${go_root}/go/bin" >> "${GITHUB_PATH}"
          "${go_root}/go/bin/go" version

      - name: Validate Go toolchain (self-hosted)
        if: ${{ runner.environment == 'self-hosted' }}
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v go >/dev/null 2>&1; then
            echo "Go is not installed on self-hosted runner. Install Go 1.25.x or higher." >&2
            exit 1
          fi
          go version

      - name: Compile Mechanical Ethics Go gate
        shell: bash
        run: |
          set -euo pipefail
          go version
          go_tmp="${RUNNER_TEMP:-/tmp}/ethicbit-go-tmp"
          go_cache="${HOME:-${RUNNER_TEMP:-/tmp}}/.cache/ethicbit-go-build"
          compile_log="${RUNNER_TEMP:-/tmp}/ethicbit-go-compile.log"
          rm -f "${compile_log}" || true
          rm -rf "${go_tmp}" || true
          mkdir -p "${go_tmp}" "${go_cache}"
          # Keep compile validation deterministic on low-memory self-hosted runners.
          export GOMAXPROCS=1
          export GOGC=20
          export GOMEMLIMIT=320MiB
          export GOFLAGS="-p=1 -trimpath"

          compile_pair() {
            local extra_gcflags="${1:-}"
            : > "${compile_log}"
            if [[ -n "${extra_gcflags}" ]]; then
              GOTMPDIR="${go_tmp}" GOCACHE="${go_cache}" GO111MODULE=on go build -trimpath -gcflags="${extra_gcflags}" ./assurance/crypto 2>&1 | tee -a "${compile_log}"
              GOTMPDIR="${go_tmp}" GOCACHE="${go_cache}" GO111MODULE=on go build -trimpath -gcflags="${extra_gcflags}" ./mechanical_ethics 2>&1 | tee -a "${compile_log}"
            else
              GOTMPDIR="${go_tmp}" GOCACHE="${go_cache}" GO111MODULE=on go build -trimpath ./assurance/crypto 2>&1 | tee -a "${compile_log}"
              GOTMPDIR="${go_tmp}" GOCACHE="${go_cache}" GO111MODULE=on go build -trimpath ./mechanical_ethics 2>&1 | tee -a "${compile_log}"
            fi
          }

          if ! compile_pair; then
            if grep -qi "signal: killed" "${compile_log}"; then
              echo "Detected compiler OOM signal. Retrying with low-memory compile flags."
              rm -rf "${go_tmp}" || true
              mkdir -p "${go_tmp}" "${go_cache}"
              export GOGC=10
              export GOMEMLIMIT=220MiB
              export GOFLAGS="-p=1 -trimpath"
              compile_pair "all=-l"
            else
              exit 1
            fi
          fi
          rm -rf "${go_tmp}" || true

      - name: Preflight sovereign crypto requirements
        shell: bash
        run: |
          set -euo pipefail
          trusted_key_sources_regex='^(trusted_secrets|trusted_remote_provider|remote_non_exportable|trusted_hsm_kms|hsm_kms)$'
          if [[ "${ETHICBIT_REQUIRE_NATIVE_HYBRID}" == "1" || "${ETHICBIT_REQUIRE_TRUSTED_KEYS}" == "1" || "${ETHICBIT_CLAIM_LEVEL}" == "freeze_grade" || "${ETHICBIT_CLAIM_LEVEL}" == "sovereign_release" ]]; then
            echo "Running strict preflight for claim level: ${ETHICBIT_CLAIM_LEVEL}"
            [[ "${ETHICBIT_REQUIRE_NATIVE_HYBRID}" == "1" ]] || { echo "Policy violation: strict claims require ETHICBIT_REQUIRE_NATIVE_HYBRID=1" >&2; exit 1; }
            [[ "${ETHICBIT_REQUIRE_TRUSTED_KEYS}" == "1" ]] || { echo "Policy violation: strict claims require ETHICBIT_REQUIRE_TRUSTED_KEYS=1" >&2; exit 1; }
            [[ "${ETHICBIT_HYBRID_CLAIM_MODE}" == "native_hybrid" ]] || { echo "Policy violation: strict claims require native_hybrid claim mode (found ${ETHICBIT_HYBRID_CLAIM_MODE})" >&2; exit 1; }
            [[ "${ETHICBIT_MLDSA_EFFECTIVE_MODE}" == "native_mldsa" || "${ETHICBIT_MLDSA_EFFECTIVE_MODE}" == "native" ]] || { echo "Policy violation: strict claims require native ML-DSA mode (found ${ETHICBIT_MLDSA_EFFECTIVE_MODE})" >&2; exit 1; }
            [[ "${ETHICBIT_ED25519_KEY_SOURCE}" =~ ${trusted_key_sources_regex} ]] || { echo "Policy violation: strict claims require trusted ED25519 key source (found ${ETHICBIT_ED25519_KEY_SOURCE})" >&2; exit 1; }
            [[ "${ETHICBIT_MLDSA_KEY_SOURCE}" =~ ${trusted_key_sources_regex} ]] || { echo "Policy violation: strict claims require trusted ML-DSA key source (found ${ETHICBIT_MLDSA_KEY_SOURCE})" >&2; exit 1; }
            [[ "${ETHICBIT_RUNNER_SUPPORTS_MLDSA}" == "true" ]] || { echo "Policy violation: strict claims require a runner with ML-DSA support" >&2; exit 1; }
            [[ "${ETHICBIT_ELIGIBLE_FOR_SOVEREIGN_RELEASE}" == "true" ]] || { echo "Policy violation: strict claims require eligible_for_sovereign_release=true" >&2; exit 1; }
          else
            echo "Preflight skipped for claim level ${ETHICBIT_CLAIM_LEVEL}."
          fi

      - name: Run constitutional fail-closed release gate
        shell: bash
        run: |
          chmod +x scripts/audit/verify_constitutional_controls.sh
          ./scripts/audit/verify_constitutional_controls.sh

      - name: Validate canonical Mechanical Ethics gate
        shell: bash
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          required = [
              sector.strip()
              for sector in os.environ.get(
                  "ETHICBIT_ME_GATE_REQUIRED_SECTORS",
                  "CORE,JUSTICIA,FINANZAS,SECURITY,TECHNICAL,LEGAL,REGULATORY",
              ).split(",")
              if sector.strip()
          ]

          gate_path = Path("results/mechanical_ethics_gate.json")
          if not gate_path.exists():
              raise SystemExit("Missing results/mechanical_ethics_gate.json")

          gate = json.loads(gate_path.read_text(encoding="utf-8"))
          if gate.get("status") != "PASS":
              raise SystemExit(
                  f"Mechanical ethics gate status is not PASS: {gate.get('status')}"
              )

          validated = set(gate.get("validated_sectors", []))
          missing_required = [sector for sector in required if sector not in validated]
          if missing_required:
              raise SystemExit(
                  "Mechanical ethics gate missing required sectors: "
                  + ",".join(missing_required)
              )

          if gate.get("l4_foundational_validated") is not True:
              raise SystemExit(
                  "Mechanical ethics gate L4 foundational validation is not true"
              )

          print("mechanical_ethics_gate: PASS (canonical + multisector)")
          PY

      - name: Materialize PQ runtime secret protection artifact
        shell: bash
        run: |
          python3 scripts/status/write_pq_runtime_secret_protection.py \
            --claim-level "${ETHICBIT_CLAIM_LEVEL}" \
            --required-sectors "${ETHICBIT_ME_GATE_REQUIRED_SECTORS}" \
            --runtime-scope "canonical_ci_runtime" \
            --protector "${ETHICBIT_RUNTIME_SECRET_PROTECTOR:-MLKEM768Protector}" \
            --policy-version "CONSTITUTIONAL_CLOSURE_ADDENDUM_V2"

      - name: Validate closure integrity (hermetic-aware)
        shell: bash
        run: |
          ETHICBIT_HERMETIC_REQUIRE_STRICT="${ETHICBIT_HERMETIC_REQUIRE_STRICT:-0}" \
          ETHICBIT_REQUIRE_GO_GATE="${ETHICBIT_REQUIRE_GO_GATE:-0}" \
          ./scripts/verify_closure_integrity.sh

      - name: Enforce crypto claim policy
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          import os
          import sys
          from pathlib import Path

          truth_path = Path("artifacts/ci/hybrid_crypto_truth.json")
          if not truth_path.exists():
              print(f"Policy violation: missing truth artifact {truth_path}", file=sys.stderr)
              raise SystemExit(1)

          truth = json.loads(truth_path.read_text(encoding="utf-8"))

          def as_bool_from_env(name: str, default: bool) -> bool:
              raw = os.environ.get(name)
              if raw is None:
                  return default
              return raw.strip().lower() in {"1", "true", "yes", "y", "on"}

          env_view = {
              "claim_level": os.environ.get("ETHICBIT_CLAIM_LEVEL", "ci_grade"),
              "require_native_hybrid": as_bool_from_env("ETHICBIT_REQUIRE_NATIVE_HYBRID", False),
              "require_trusted_keys": as_bool_from_env("ETHICBIT_REQUIRE_TRUSTED_KEYS", False),
              "hybrid_claim_mode": os.environ.get("ETHICBIT_HYBRID_CLAIM_MODE", "unknown"),
              "mldsa_effective_mode": os.environ.get("ETHICBIT_MLDSA_EFFECTIVE_MODE", "unknown"),
              "ed25519_key_source": os.environ.get("ETHICBIT_ED25519_KEY_SOURCE", "unknown"),
              "mldsa_key_source": os.environ.get("ETHICBIT_MLDSA_KEY_SOURCE", "unknown"),
              "ephemeral_keys_used": as_bool_from_env("ETHICBIT_EPHEMERAL_KEYS_USED", True),
              "runner_supports_mldsa": as_bool_from_env("ETHICBIT_RUNNER_SUPPORTS_MLDSA", False),
              "eligible_for_sovereign_release": as_bool_from_env("ETHICBIT_ELIGIBLE_FOR_SOVEREIGN_RELEASE", False),
              "key_provenance_mode": os.environ.get("ETHICBIT_KEY_PROVENANCE_MODE", "unknown"),
              "crypto_claim_tier": os.environ.get("ETHICBIT_CRYPTO_CLAIM_TIER", "unspecified"),
              "release_class": os.environ.get("ETHICBIT_RELEASE_CLASS", "branch_ci"),
          }

          compare_keys = [
              "claim_level",
              "require_native_hybrid",
              "require_trusted_keys",
              "hybrid_claim_mode",
              "mldsa_effective_mode",
              "ed25519_key_source",
              "mldsa_key_source",
              "ephemeral_keys_used",
              "runner_supports_mldsa",
              "eligible_for_sovereign_release",
              "key_provenance_mode",
              "crypto_claim_tier",
              "release_class",
          ]

          mismatches = []
          for key in compare_keys:
              if truth.get(key) != env_view[key]:
                  mismatches.append((key, truth.get(key), env_view[key]))

          if mismatches:
              print("Policy violation: hybrid_crypto_truth.json is out of sync with environment.", file=sys.stderr)
              for key, truth_value, env_value in mismatches:
                  print(
                      f"  - {key}: truth={truth_value!r} env={env_value!r}",
                      file=sys.stderr,
                  )
              raise SystemExit(1)

          claim_level = str(truth.get("claim_level", "ci_grade"))
          require_native_hybrid = bool(truth.get("require_native_hybrid", False))
          require_trusted_keys = bool(truth.get("require_trusted_keys", False))
          hybrid_claim_mode = str(truth.get("hybrid_claim_mode", "unknown"))
          mldsa_effective_mode = str(truth.get("mldsa_effective_mode", "unknown"))
          ed25519_key_source = str(truth.get("ed25519_key_source", "unknown"))
          mldsa_key_source = str(truth.get("mldsa_key_source", "unknown"))
          ephemeral_keys_used = bool(truth.get("ephemeral_keys_used", True))
          runner_supports_mldsa = bool(truth.get("runner_supports_mldsa", False))
          eligible_for_sovereign_release = bool(truth.get("eligible_for_sovereign_release", False))
          trusted_sources = {
              "trusted_secrets",
              "trusted_remote_provider",
              "remote_non_exportable",
              "trusted_hsm_kms",
              "hsm_kms",
          }

          def is_trusted_source(value: str) -> bool:
              return value in trusted_sources

          print(
              "POLICY_CONTEXT:",
              {
                  "claim_level": claim_level,
                  "require_native_hybrid": require_native_hybrid,
                  "require_trusted_keys": require_trusted_keys,
                  "hybrid_claim_mode": hybrid_claim_mode,
                  "mldsa_effective_mode": mldsa_effective_mode,
                  "ed25519_key_source": ed25519_key_source,
                  "mldsa_key_source": mldsa_key_source,
                  "ephemeral_keys_used": ephemeral_keys_used,
                  "runner_supports_mldsa": runner_supports_mldsa,
                  "eligible_for_sovereign_release": eligible_for_sovereign_release,
              },
          )

          # Base consistency checks for effective crypto truth
          if (
              ephemeral_keys_used
              and is_trusted_source(ed25519_key_source)
              and is_trusted_source(mldsa_key_source)
          ):
              print(
                  "Policy violation: ephemeral_keys_used=true but both key sources are trusted.",
                  file=sys.stderr,
              )
              raise SystemExit(1)
          if (
              not ephemeral_keys_used
              and (ed25519_key_source == "ephemeral_runner" or mldsa_key_source == "ephemeral_runner")
          ):
              print(
                  "Policy violation: ephemeral_keys_used=false but an ephemeral key source is declared.",
                  file=sys.stderr,
              )
              raise SystemExit(1)

          # Trusted-key requirement enforcement (enabled on main and sovereign-grade claims)
          if require_trusted_keys:
              if not is_trusted_source(ed25519_key_source) or not is_trusted_source(mldsa_key_source):
                  print(
                      "Policy violation: require_trusted_keys=true requires trusted key sources on both channels.",
                      file=sys.stderr,
                  )
                  raise SystemExit(1)
              if ephemeral_keys_used:
                  print(
                      "Policy violation: require_trusted_keys=true does not allow ephemeral keys.",
                      file=sys.stderr,
                  )
                  raise SystemExit(1)

          # Native-hybrid requirement enforcement
          if require_native_hybrid:
              if hybrid_claim_mode != "native_hybrid":
                  print(
                      f"Policy violation: require_native_hybrid=true but hybrid_claim_mode={hybrid_claim_mode}.",
                      file=sys.stderr,
                  )
                  raise SystemExit(1)
              if mldsa_effective_mode != "native_mldsa":
                  print(
                      f"Policy violation: require_native_hybrid=true but mldsa_effective_mode={mldsa_effective_mode}.",
                      file=sys.stderr,
                  )
                  raise SystemExit(1)

          # Freeze/release-grade enforcement
          if claim_level in {"freeze_grade", "sovereign_release"}:
              if not is_trusted_source(ed25519_key_source) or not is_trusted_source(mldsa_key_source):
                  print(
                      f"Policy violation: {claim_level} requires trusted key sources on both channels.",
                      file=sys.stderr,
                  )
                  raise SystemExit(1)
              if ephemeral_keys_used:
                  print(
                      f"Policy violation: {claim_level} does not allow ephemeral keys.",
                      file=sys.stderr,
                  )
                  raise SystemExit(1)
              if hybrid_claim_mode != "native_hybrid" or mldsa_effective_mode != "native_mldsa":
                  print(
                      f"Policy violation: {claim_level} requires native hybrid mode.",
                      file=sys.stderr,
                  )
                  raise SystemExit(1)
              if not runner_supports_mldsa:
                  print(
                      f"Policy violation: {claim_level} requires ML-DSA support on runner.",
                      file=sys.stderr,
                  )
                  raise SystemExit(1)
              if not eligible_for_sovereign_release:
                  print(
                      f"Policy violation: {claim_level} requires eligible_for_sovereign_release=true.",
                      file=sys.stderr,
                  )
                  raise SystemExit(1)

          print("Crypto claim policy enforcement PASS")
          PY

      - name: Hybrid-sign provenance payload
        run: |
          python3 - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path
          from scripts.crypto.jcs_rfc8785 import canonicalize_bytes

          truth_path = Path("artifacts/ci/hybrid_crypto_truth.json")
          if not truth_path.exists():
            raise SystemExit(f"missing truth artifact: {truth_path}")
          truth_bytes = truth_path.read_bytes()
          hybrid_crypto_truth = json.loads(truth_bytes.decode("utf-8"))
          hybrid_crypto_truth_sha256 = hashlib.sha256(truth_bytes).hexdigest()

          gate_path = Path("results/mechanical_ethics_gate.json")
          if not gate_path.exists():
            raise SystemExit(f"missing mechanical ethics gate artifact: {gate_path}")
          gate_bytes = gate_path.read_bytes()
          mechanical_ethics_gate = json.loads(gate_bytes.decode("utf-8"))
          mechanical_ethics_gate_sha256 = hashlib.sha256(gate_bytes).hexdigest()

          hermetic_path = Path("results/hermetic_build_report.json")
          if not hermetic_path.exists():
            raise SystemExit(f"missing hermetic build report artifact: {hermetic_path}")
          hermetic_bytes = hermetic_path.read_bytes()
          hermetic_report = json.loads(hermetic_bytes.decode("utf-8"))
          hermetic_report_sha256 = hashlib.sha256(hermetic_bytes).hexdigest()

          pq_runtime_path = Path("results/pq_runtime_secret_protection.json")
          if not pq_runtime_path.exists():
            raise SystemExit(f"missing pq runtime secret protection artifact: {pq_runtime_path}")
          pq_runtime_bytes = pq_runtime_path.read_bytes()
          pq_runtime_report = json.loads(pq_runtime_bytes.decode("utf-8"))
          pq_runtime_sha256 = hashlib.sha256(pq_runtime_bytes).hexdigest()

          if mechanical_ethics_gate.get("status") != "PASS":
            raise SystemExit(
              f"mechanical ethics gate must be PASS before signing, got {mechanical_ethics_gate.get('status')}"
            )
          if os.environ.get("ETHICBIT_HERMETIC_REQUIRE_STRICT", "0").lower() in {"1", "true", "yes"}:
            if hermetic_report.get("status") != "PASS_STRICT_HERMETIC":
              raise SystemExit(
                "strict hermetic mode requires PASS_STRICT_HERMETIC report status before signing"
              )

          payload = {
            "artifactType": "ethicbit_ci_attestation_payload",
            "repository": "${{ github.repository }}",
            "ref": "${{ github.ref }}",
            "sha": "${{ github.sha }}",
            "run_id": "${{ github.run_id }}",
            "ed25519_key_source": os.environ.get("ETHICBIT_ED25519_KEY_SOURCE", "unknown"),
            "mldsa_key_source": os.environ.get("ETHICBIT_MLDSA_KEY_SOURCE", "unknown"),
            "mldsa_effective_mode": os.environ.get("ETHICBIT_MLDSA_EFFECTIVE_MODE", "unknown"),
            "hybrid_claim_mode": os.environ.get("ETHICBIT_HYBRID_CLAIM_MODE", "unknown"),
            "crypto_mode_effective": os.environ.get("ETHICBIT_CRYPTO_MODE_EFFECTIVE", "unknown"),
            "mldsa_mode": os.environ.get("ETHICBIT_MLDSA_MODE", "unknown"),
            "key_provenance_mode": os.environ.get("ETHICBIT_KEY_PROVENANCE_MODE", "unknown"),
            "crypto_claim_tier": os.environ.get("ETHICBIT_CRYPTO_CLAIM_TIER", "unspecified"),
            "audit_require_hybrid": os.environ.get("ETHICBIT_AUDIT_REQUIRE_HYBRID", "0"),
            "release_class": os.environ.get("ETHICBIT_RELEASE_CLASS", "branch_ci"),
            "claim_level": os.environ.get("ETHICBIT_CLAIM_LEVEL", "ci_grade"),
            "require_native_hybrid": os.environ.get("ETHICBIT_REQUIRE_NATIVE_HYBRID", "0").lower() in {"1", "true", "yes"},
            "require_trusted_keys": os.environ.get("ETHICBIT_REQUIRE_TRUSTED_KEYS", "0").lower() in {"1", "true", "yes"},
            "ephemeral_keys_used": os.environ.get("ETHICBIT_EPHEMERAL_KEYS_USED", "true").lower() == "true",
            "runner_supports_mldsa": os.environ.get("ETHICBIT_RUNNER_SUPPORTS_MLDSA", "false").lower() == "true",
            "eligible_for_sovereign_release": os.environ.get("ETHICBIT_ELIGIBLE_FOR_SOVEREIGN_RELEASE", "false").lower() == "true",
            "hybrid_crypto_truth_sha256": hybrid_crypto_truth_sha256,
            "hybrid_crypto_truth": hybrid_crypto_truth,
            "mechanical_ethics_gate_path": str(gate_path),
            "mechanical_ethics_gate_sha256": mechanical_ethics_gate_sha256,
            "mechanical_ethics_gate_status": mechanical_ethics_gate.get("status", "UNKNOWN"),
            "mechanical_ethics_gate_mode": mechanical_ethics_gate.get("mode", "UNKNOWN"),
            "mechanical_ethics_gate_claim_level_ceiling": mechanical_ethics_gate.get("claim_level_ceiling", "L3"),
            "mechanical_ethics_gate_l4_foundational_validated": mechanical_ethics_gate.get("l4_foundational_validated", False),
            "mechanical_ethics_gate_required_sectors": mechanical_ethics_gate.get("required_sectors", []),
            "mechanical_ethics_gate_validated_sectors": mechanical_ethics_gate.get("validated_sectors", []),
            "mechanical_ethics_gate_l4_blockers": mechanical_ethics_gate.get("l4_blockers", []),
            "hermetic_build_report_path": str(hermetic_path),
            "hermetic_build_report_sha256": hermetic_report_sha256,
            "hermetic_build_status": hermetic_report.get("status", "UNKNOWN"),
            "hermetic_build_mode": hermetic_report.get("hermetic_mode", "unknown"),
            "hermetic_build_network_mode": hermetic_report.get("network_mode", "unknown"),
            "hermetic_build_base_image_ref": hermetic_report.get("base_image_ref", "unknown"),
            "hermetic_build_pinned_base_image_verified": hermetic_report.get("controls", {}).get("pinned_base_image_verified", False),
            "pq_runtime_secret_protection_path": str(pq_runtime_path),
            "pq_runtime_secret_protection_status": pq_runtime_report.get("status", "UNKNOWN"),
            "pq_runtime_secret_protection_sha256": pq_runtime_sha256,
            "pq_runtime_secret_protector": pq_runtime_report.get("protector", "UNKNOWN"),
            "pq_runtime_secret_reference": pq_runtime_report.get("secret_reference", "UNKNOWN"),
            "pq_runtime_evidence_ref": pq_runtime_report.get("evidence_ref", "UNKNOWN"),
          }
          Path("provenance/payload.json").write_text(
            json.dumps(payload, indent=2, ensure_ascii=False) + "\n",
            encoding="utf-8"
          )
          Path("provenance/payload.jcs.json").write_bytes(canonicalize_bytes(payload))
          PY

          extra_sign_args=()
          if [[ "${ETHICBIT_REQUIRE_NATIVE_HYBRID}" == "1" ]]; then
            extra_sign_args+=(--require-native-hybrid)
          fi

          python3 scripts/crypto/hybrid_sign.py \
            provenance/payload.json \
            --output attestation/hybrid_signature_set.json \
            --risk-mode STANDARD \
            --ed25519-sign-cmd "$ETHICBIT_ED25519_SIGN_CMD" \
            --mldsa-sign-cmd "$ETHICBIT_MLDSA_SIGN_CMD" \
            --ed25519-key-id "$ETHICBIT_ED25519_KEY_ID" \
            --mldsa-key-id "$ETHICBIT_MLDSA_KEY_ID" \
            --crypto-truth-json artifacts/ci/hybrid_crypto_truth.json \
            --claim-level "$ETHICBIT_CLAIM_LEVEL" \
            "${extra_sign_args[@]}"

      - name: Verify hybrid signatures
        run: |
          python3 scripts/crypto/hybrid_verify.py \
            --payload provenance/payload.jcs.json \
            --signature-set attestation/hybrid_signature_set.json \
            --risk-mode STANDARD \
            --ed25519-verify-cmd "$ETHICBIT_ED25519_VERIFY_CMD" \
            --mldsa-verify-cmd "$ETHICBIT_MLDSA_VERIFY_CMD"

      - name: Run full audit
        run: |
          chmod +x ./audit.sh
          echo "ETHICBIT_AUDIT_REQUIRE_HYBRID=${ETHICBIT_AUDIT_REQUIRE_HYBRID}"
          echo "ETHICBIT_CRYPTO_MODE_EFFECTIVE=${ETHICBIT_CRYPTO_MODE_EFFECTIVE}"
          echo "ETHICBIT_KEY_PROVENANCE_MODE=${ETHICBIT_KEY_PROVENANCE_MODE}"
          echo "ETHICBIT_MLDSA_MODE=${ETHICBIT_MLDSA_MODE}"
          echo "ETHICBIT_CRYPTO_CLAIM_TIER=${ETHICBIT_CRYPTO_CLAIM_TIER}"
          ETHICBIT_AUDIT_RISK_MODE=STANDARD \
          ETHICBIT_AUDIT_REQUIRE_HYBRID="${ETHICBIT_AUDIT_REQUIRE_HYBRID}" \
          ./audit.sh

      - name: Upload GitHub provenance attestation
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: |
            provenance/payload.jcs.json
            attestation/hybrid_signature_set.json

      - name: Upload CI artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ethicbit-slsa-hybrid-attestation-${{ github.run_id }}
          path: |
            provenance/payload.json
            provenance/payload.jcs.json
            attestation/hybrid_signature_set.json
            artifacts/history/swarm/official_operational_status.json
            results/GATE_REPORT.json
            results/constitutional_controls_report.json
            results/mechanical_ethics_gate.json
            results/pq_runtime_secret_protection.json
            results/hermetic_build_report.json
            results/key_posture_report.json
            artifacts/history/swarm/triple_public_anchor_live_verification.json
            artifacts/ci/signing_environment_report.json
            artifacts/ci/hybrid_mode_effective.json
            artifacts/ci/hybrid_crypto_truth.json
