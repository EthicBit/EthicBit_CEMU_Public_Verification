# EthicBit/CEMU - dependencies
# Last verified working: 2026-04-28

# Preserved from earlier baseline (used by analysis/utility scripts)
numpy>=1.26.0
PyYAML>=6.0.2
requests>=2.31.0

# L5 verification dependencies (pinned)
# Pinned to prevent silent breakage from upstream API changes.
# (Already happened once in this codebase: eth_account messageHash -> message_hash.)
#
# When upgrading these deps, do it intentionally:
#   pip install -r requirements.txt --upgrade
#   python3 scripts/core/verify_l5_full_chain.py        # must exit 0
#   python3 scripts/core/test_tampering_resistance.py   # must exit 0
# Then commit the new versions with the verifier output as evidence.
eth_account==0.13.7
eth_utils==6.0.0
eth_abi==5.2.0
web3==7.15.0
rlp==4.1.0
ckzg==2.1.7
